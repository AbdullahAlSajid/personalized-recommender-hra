# rec•sys web app prototype

This is a code bundle for rec•sys web app prototype. The original project is available at https://www.figma.com/design/aToxM3uM14k9A0yfuQouwM/rec%E2%80%A2sys-web-app-prototype.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
